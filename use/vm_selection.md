Selecting which VMs to Monitor
====================

Introduction
----------------
zPlanner does not need to monitor VM's that will not be replicated. Using the zPlanner Web UI you can select which VMs to monitor.

### More to come.